# Movies.db Backend
Movies.db is a movie database that has the most up-to-date information about trending movies, including streaming options. This is a Node.js REST API back-end application created with an Express.js server. The server is deployed on Render with a free-tier license.

```
NOTE: The initial loading time can take up to a minute due to auto scale down after a certain period of inactivity.
``` 
